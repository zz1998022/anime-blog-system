name: build client

on:
  push:
    branches:
      - dev
    path:
      - client

jobs:
  client:
    runs-on: ubuntu-latest
    steps:
      - name: Install Package
        working-directory: ./client
        run: yarn
      - name: Build Client
        working-directory: ./client
        run: yarn build